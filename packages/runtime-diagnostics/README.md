# Runtime diagnostics

English | [中文](README.zh.md)

Registry infrastructure for package-owned runtime invariant checks. Group rules: [group](../AGENTS.md), [root](../../AGENTS.md#conventions).

| Package | Role |
|---|---|
| [`invariants/`](invariants/README.md) | Configurable `InvariantRegistry` service (`ctx.invariants`) every package's `./invariant` companion registers under |
