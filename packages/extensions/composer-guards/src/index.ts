/**
 * Shared composer guards, node half. Pure library row: the empty apply exists
 * so the package is an ordinary cordis.yml / Loader entry, while the browser
 * half supplies the guard helpers as a module-table row consumers request
 * through `dsh.client.external`.
 * @module @deepseek-ai/dsh-client-composer-guards
 */

/** Host plugin body — no host-side behavior for this library row. */
export function apply(): void {}
