/**
 * Composer draft token budget, browser half (scaffold). The estimator, the
 * dock readout component, and the dictionaries land in the next commit.
 * @module @deepseek-ai/dsh-client-draft-budget/client
 */

/** Required services for the dock readout (slots and locale). */
export const inject = ['slots', 'locale']

/** Client plugin body — inert until the core lands. */
export function apply(): void {}
