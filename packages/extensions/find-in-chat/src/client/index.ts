/**
 * In-conversation find bar, browser half (scaffold). The search engine,
 * controller, and the shell.overlay bar entry land in the next commit.
 * @module @deepseek-ai/dsh-client-find-in-chat/client
 */

/** Required services for the find bar (slots, locale, session list). */
export const inject = ['slots', 'locale', 'sessions']

/** Client plugin body — inert until the core lands. */
export function apply(): void {}
